### folder này dành cho redux

# 3 flie rootReducer and authReducer, userReducer

`rootReducer` =>> là file gom các reducer
`authReducer` =>> là file dùng xác thực
`userReducer` =>> lưu thông tin của user